---
title: "Tutorial 2 Assignment"
author: "Kevin Callison"
output: pdf_document
---

We can type normally and this will show up when we compile the pdf.

We can add R commands like 

```{r}
library(nycflights13)
library(dplyr)
library(knitr)

flights
```

Now let's see what that does.

```{r}
View(flights)
glimpse(flights)
```